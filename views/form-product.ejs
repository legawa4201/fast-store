<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <title>Fast Store Control Panel - New Product</title>
</head>

<body>
    <div class="d-flex">
        <%- include('partials/sidebar') %>
        <div style="width: 90%;" class="d-flex p-3 vh-100 justify-content-center align-items-center">
            <form action="/products/add-product" method="post" class="d-flex flex-column p-5 bg-dark row gy-2 text-white" style="height: 90%; width: 95%;">
                <div>
                    <h3>Produk Baru</h3>
                </div>
                <div class="d-flex">
                    <%if(errors !== undefined) {%>
                        <%errors.forEach(function(err) {%>
                            <p class="text-danger"><%=err%></p>
                        <%})%>
                    <%}%>
                </div>

                <label>Nama Produk</label>
                <input type="text" name="name" class="form-control">

                <label>Harga Produk</label>
                <input type="number" name="price" class="form-control">

                <label>Kategori</label>
                <select name="category_id" class="form-select text-center">
                    <option value="" selected disabled>SELECT CATEGORY</option>
                    <%{%>
                        <%categories.forEach(function(cat) {%>
                            <option value="<%=cat.category_id%>">
                                <%=cat.category_name%>
                            </option>
                        <%})%>
                    <%}%>
                </select>
                
                <label>status</label>
                <select name="status_id" class="form-select text-center">
                    <option value="" selected disabled>SELECT STATUS</option>
                    <%{%>
                        <%status.forEach(function(stat) {%>
                            <option value="<%=stat.status_id%>">
                                <%=stat.status_name%>
                            </option>
                        <%})%>
                    <%}%>
                </select>

                <button type="submit" class="btn btn-light w-25">Tambah Produk</button>
            </form>
        </div>
    </div>
</body>

</html>